import { NumeroTarjetaPipe } from './numero-tarjeta.pipe';

describe('NumeroTarjetaPipe', () => {
  it('create an instance', () => {
    const pipe = new NumeroTarjetaPipe();
    expect(pipe).toBeTruthy();
  });
});
